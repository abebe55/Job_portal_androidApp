# Local development — points to the live Render backend
# For production deployment on Vercel, set this in the Vercel dashboard:
#   Settings → Environment Variables → NEXT_PUBLIC_API_URL
NEXT_PUBLIC_API_URL=https://job-portal-androidapp.onrender.com/api
